import styled from 'styled-components'

export const EpisodeStyles = styled.section`
  padding: 0 0 calc(var(--gutter)/2);
`;

export const EpisodeTitleStyles = styled.span`
  padding: 0 calc(var(--gutter)/2);
`;

export const EpisodeDescriptionStyles = styled.div`
  padding: calc(var(--gutter)/2) 0 0;
`;
